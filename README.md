# WomanLive 拡張機能 (非公式アドオン)

動画管理ソフト **WomanLive** に、個人利用向けの機能を追加する非公式アドオンです。
**WomanLive 本体のファイルは一切変更しません**。

> 本アドオンは WomanLive 本体を含みません。WomanLive がインストール・セットアップ済みの
> 環境に追加してください。WomanLive 本体は MIT ライセンス (Copyright 2016 woman projects)。

## 追加される主な機能
- カバー画像（指定フォルダから動画名と一致するものを自動表示）
- メタデータの拡張（評価・動画の表示名・品番・公開日・シリーズ・メーカー・レーベル・監督・出演者・ジャンル）
- 関連動画をメタデータの類似度で選定（重み調整可）
- 出演者ページ／出演者一覧
- メーカー一覧（メーカー画像・キャッチコピー・TOP10タグ・レーベル一覧・FANZAリンク）
- シリーズ一覧
- タグ一覧
- 公開年月別カレンダー
- 評価ソート・評価絞り込み
- 検索結果のサイドメニュー（評価スライダー・ジャンル・タグ・メーカー・出演者・シリーズで絞り込み）
- モバイルからの動画の端末保存・他アプリでの再生
- コマ送り（`.` / `,` キー・Shift+ホイール・スマホ/タブレットのボタン）
- メタデータの一括置換（タグ・ジャンル・出演者・シリーズ名・メーカー・レーベル）
- 一覧ページ（シリーズ・出演者・タグ）からの一括削除
- 参照されていない拡張データの削除（本体のクリーンアップに連動）
- 出演者タグ機能、出演者タグの自動付与ルール
- DMM(FANZA) 商品API・メーカーAPIによるメタデータ自動設定
- ブックマーク機能
等。機能の詳細は `server/ext/README.md` を参照してください。

## 動作要件
- セットアップ済みの WomanLive（同梱の Node.js / ffmpeg / better-sqlite3 / sharp を利用）
- 追加の部品インストールは不要（新しい npm パッケージは使いません）
- DMM 商品検索とカバー画像の取得時のみ、外部通信を使用

## 導入方法
1. [Release](https://github.com/MT-Project/womanlive-ext/releases)から最新の **`womanlive-ext_X.X.X.zip`**（X.X.X はバージョン）をダウンロードして解凍
2. 解凍した中身（**`server` フォルダ**と **`womanlive-ext.bat`**）を、WomanLive の**ルートフォルダ**（`womanlive.bat` と同じ場所）へ**そのままコピー**
   `server` フォルダは既存の `server` に統合され、中の `ext` だけが追加されます（既存ファイルは上書きされません）
3. WomanLive を一度終了し、**`womanlive-ext.bat`** から起動
（以後、拡張機能つきで使うときは `womanlive-ext.bat` から起動してください）

## アンインストール
- `womanlive-ext.bat` ではなく、通常の `womanlive.bat` から起動すれば拡張機能は無効になります。
- 完全に削除する場合は `server/ext` フォルダと `womanlive-ext.bat` を削除してください。
- 追加データも消す場合は、設定画面のバックアップでエクスポートしておいた上で、
  `videos.db` の次のテーブルを削除（DROP）してください：
  `ext_video_meta` / `ext_performers` / `ext_video_cover` / `ext_tag_thumb` /
  `ext_makers` / `ext_bookmark_folders` / `ext_bookmarks`
  （`ext_related` は再計算できるキャッシュなので任意。
  `settings` テーブルの `ext_` で始まるキーも拡張用の設定です）

## データ・バックアップ
追加した情報（メタデータ・出演者・画像・ブックマーク・設定）は WomanLive の `videos.db` 内の
**新規テーブル**に保存され、既存データは変更しません。設定画面の「バックアップ」から JSON で
書き出し／復元できます。

## ライセンス
本アドオンは MIT ライセンスです（`LICENSE` 参照）。WomanLive 本体（MIT, Copyright 2016 woman projects）とは別の独立したプログラムであり、本体のコードは同梱していません。
"WomanLive" の名称は対象ソフトの識別のために使用しているもので、woman projects による
公式の承認・提供を受けたものではありません。
[WomanLive 本体配布先](https://womanprojects.web.fc2.com)

## 配布物の構成
```
womanlive-ext/
  README.md            このファイル
  LICENSE              MIT ライセンス
  womanlive-ext.bat    拡張機能用起動ファイル（ルートへコピー）
  server/              ルートへコピー（既存 server に統合される）
    ext/               アドオン本体
      preload.js       本体無改変で拡張機能をプリロードするためのファイル
      index.js db.js inject.js
      routes/  public/ サーバーAPI / ブラウザ側UI
      README.md        機能詳細
```

## 更新履歴
### v1.6.5 (2026-09-02)
- **絞り込みサイドメニューに開閉ボタンを追加**: 検索結果の左上に「絞り込み」ボタンを置きました。スクロールしても位置は変わりません。押すたびにサイドメニューを開閉し、既定は PC が開く・モバイルが閉じるです。切り替えた状態はその端末に記憶します。スマホなど幅の狭い画面では、一覧を押し出さずに重ねて表示し、背景をタップすると閉じます。
- **評価の絞り込みをサイドメニューへ移動**: 並び替えボタンの右にあった ★1〜★5 を廃止し、サイドメニューの一番上（ジャンルの上）に 0〜5 の範囲スライダーを置きました。両端をドラッグして「2〜4」のように範囲で絞り込めます。他の絞り込みを保ったまま評価だけ変更できます。
- **モバイルで「端末に保存」「アプリで開く」を追加**: 動画ページの「フォルダーを開く」「アプリで再生」はサーバー機側を操作するもので、スマホからは表示されませんでした。タッチ端末では代わりにこの2つを表示します。「端末に保存」はブラウザの標準ダウンロードで動画を保存し、「アプリで開く」は OS のアプリへ渡して再生します（Android はアプリ選択、iOS などは別タブで開きます）。

<details>
<summary>過去の更新履歴（v1.6.4 以前）を表示</summary>

### v1.6.4 (2026-08-21)
- **検索結果にサイドメニュー（絞り込み）を追加**: 検索結果の左側に、上から ジャンル・タグ・メーカー・出演者・シリーズ を表示します。並びは今の検索結果に対して件数の多い順で、上位5件を出し、残りは「すべて表示」で開きます。クリックするとその条件が今の検索に加わり、もう一度押すと外れます。絞り込むたびに候補と件数も計算し直します。検索語が空か `@` で始まる項目検索のときに表示します（本家のキーワード検索は条件の組み立てが違うため対象外）。
- **ブックマークの検索をフォルダ名に変更**: `@bookmark:"名前"` で検索します。ブックマーク一覧のリンクもこの形式になりました。これまでのフォルダID指定（`@bookmark:2`）も引き続き使えます。
- **表示動画名にドットが含まれると途中までしか表示されない問題を修正**: 「〇〇vol.3_1」のような名前が「〇〇vol」で切れていました。本家がファイル名の最後のドット以降を拡張子として落とすためで、拡張側が表示動画名を渡すときに元ファイルの拡張子を付けるようにしました。動画ページ・検索結果・関連動画・ブラウザのタブに効きます。
- **設定のフォルダ参照でドライブレターが二重に入る問題を修正**: 「拡張機能：カバー画像」の参照からフォルダを選ぶと `D:\D:\Cover` のようになっていました。

### v1.6.3 (2026-08-20)
- **メーカー一覧を追加**: ホームと右上メニューの「公開カレンダー」の下から開きます。メーカーごとに、画像・キャッチコピー・動画本数・平均評価・TOP10タグ・レーベル一覧を横長のリストで表示します。画像と名前をクリックするとそのメーカーで検索、タグをクリックするとメーカーとタグの両方で絞り込み、レーベル名をクリックするとそのレーベルで検索します。レーベル未設定の作品は「レーベルなし」として、本数によらず一覧の末尾に置きます。画像とキャッチコピーはその場で登録・編集できます。
- **メーカー情報の取得を追加**: メーカー一覧の「⋮」から、FANZA のメーカー検索APIで読み仮名とリストページURLを取得します。設定前に確認画面を出します。名前が完全一致しないときは候補を出すので、そこから選べます。リストページURLを登録すると、行の右下に FANZA へのリンクが出ます。
- **一覧ページからの一括削除を追加**: シリーズ一覧・出演者一覧・タグ一覧で、検索結果の一括操作と同じ手順で複数を選び、全動画のメタデータからまとめて取り除けます。削除する名前は確認画面で一覧表示します。動画そのものは削除しません。タグを削除すると、プリセットとグループ分けの設定からも消えます。
- **拡張の各ページにタイトルを表示**: メーカー一覧・シリーズ一覧・出演者一覧・出演者ページ・タグ一覧・公開カレンダー・ブックマークで、ブラウザのタブに本家と同じ「〇〇 - WomanLive」を表示します。
- **DMM(FANZA) 商品検索の取りこぼしを修正**: FANZA 側の商品番号の頭に数字が付く作品が見つかりませんでした（例: `TOTTE-283` の商品番号は `1totte00283`）。品番をそのまま検索する引き方と、`totte00283` の形で厳密に一致させる引き方の両方が空振りするためです。どちらも0件のときは `totte00283` をキーワードとして検索する手順を足しました。
- **DMM(FANZA) の商品情報を反映するとき、取得できなかった項目で既存の値を消さないように修正**: 出演者情報を持たない商品を反映すると、手で入れた出演者が消えていました。シリーズ名・メーカー・レーベル・作品監督・公開日・表示動画名も同じです。確認画面に出ていない項目は変更しません。

### v1.6.2 (2026-08-16)
- **タッチ操作でのコマ送りを追加**: スマホやタブレットで動画を一時停止すると、プレイヤーに半透明のボタンを表示します。左端で1コマ戻し、中央で再生、右端で1コマ送りです。再生中とマウス操作の端末では出しません。本家の横スワイプ（滑らせた距離で早送り・巻き戻しする操作）はこれまでどおりです。

### v1.6.1 (2026-08-04)
- **メタデータの一括置換を追加**: 設定の「タグ置換」を「メタデータ置換」に差し替え、タグに加えてジャンル・出演者・シリーズ名・メーカー・レーベルも置換できるようにしました。タグを置換すると、プリセットとグループ分けの設定も一緒に変わります。
- **参照されていない拡張データの削除を追加**: 動画ファイルを消したあとも残っていた拡張機能のデータ（メタデータ・カバー画像・ブックマーク・関連動画キャッシュ）を削除します。本体の「メンテナンス」のクリーンアップを実行したときも一緒に消えます。設定画面から件数を見て単独で実行することもできます。
- **検索結果に「動画表示名で表示する」の切り替えを追加**: 右上の「表示設定」から切り替えます。OFF にすると表示動画名を使わず、ファイル名で表示します（既定はON）。
- **出演者タグの自動付与を修正**: 出演者側にその項目が登録されていないとき、条件に合ったものとして扱われることがありました（カップ数の「≦ D」が未登録の出演者全員に付くなど）。未登録の場合は付与しないようにしました。

### v1.6.0 (2026-07-28)
- **コマ送りを追加**: 動画ページで `.` を押すと1コマ送り、`,` で1コマ戻しです。プレイヤー上の Shift+ホイールでも操作できます（従来のホイール5秒送りはそのまま）。操作すると時刻とフレーム番号を画面上部に表示します。ファイルをそのまま再生できる動画が対象で、変換再生の動画では使えません。

### v1.5.6 (2026-07-28)
- **DMM(FANZA) の商品検索で、通販（DVD）の作品も見つかるように修正**: これまでは動画の売り場だけを検索していたため、DVD でしか出ていない作品が0件になっていました（例: `ODVHJ-067`）。動画で見つからないときは通販も検索します。動画側で見つかる品番の結果はこれまでと同じです。
- **通販の作品のカバー画像を大きいものに変更**: 通販は数百ピクセルのサムネイルしか返さないため、同じ作品の大判画像を探して使うようにしました。大判が無い作品では従来の画像のままです。

### v1.5.5 (2026-07-28)
- **DMM(FANZA) の商品情報を反映するとき、ジャンルをマージするように変更**: これまでは取得したジャンルで置き換えていたため、手で足したジャンルが消えていました。既存のジャンルを残したまま、取得分のうち未登録のものだけを追加します。確認ダイアログのジャンル欄にも反映後の一覧を表示します。

### v1.5.4 (2026-07-28)
- **タグ設定画面の表記を変更**: 「タグ編集...」ボタンを「プリセット編集...」に変更しました。編集中はダイアログの見出しも「プリセット編集...」になり、その右の「？」でグループ化（`#` / `##`）の書き方を確認できます。
- **リスト表示の出演者名をリンク化**: 出演者名から出演者ページへ移動できます。中クリックで新しいタブにも開けます。
- **動画ページのメタデータを中クリックに対応**: ジャンル・出演者・品番・シリーズ名・メーカー・レーベル・作品監督の各リンクと、「−」付き虫めがね（除外検索）を、中クリックで新しいタブに開けるようにしました。

### v1.5.3 (2026-07-26)
- **検索結果カードの評価・スクショ数・ブックマークの表示位置を変更**: 大きいグリッド/小さいグリッド/リストはサムネイルからタイトル下の行へ移動（評価→スクショ数→ブックマークの順）。ポスターはサムネイル上のまま、プレビュー再生前でも常に表示されるようにしました。
- **リスト表示に出演者名を追加**: 出演者が登録済みの動画は、評価行の下・タグ行の上に出演者名（画像なし）を表示。

### v1.5.2 (2026-07-26)
- **WomanLive ver.260725 テスト版への対応**: 本体アップデートで内部構造が変わり、検索結果カードの表示拡張（メーカー・評価・フォルダ名）、リスト表示の「⋮」メニュー、カレンダーの中クリック、スクリーンショット表示サイズ変更が動かなくなっていた問題を修正しました。見た目や操作方法に変更はありません。

### v1.5.1 (2026-07-11)
- **出演者ページの関連スクリーンショットを改善**: 表示サイズを関連動画のサムネイルと統一。見出しの右に「最近」「ランダム」の切り替えボタンを追加し、ランダムは押すたびに8件を抽選し直します。
- **タグ一覧に「プリセット順」ソートを追加**: タグプリセットの並び順で表示し、プリセットにグループ分け（`#`）があれば、グループ名の見出しで区切って表示します。
- **一部ページの並び替え選択を記憶**: シリーズ一覧・出演者一覧・タグ一覧の並び替えは、ページを離れて戻ってもリセットされず、選択した状態を維持します。

### v1.5.0 (2026-07-02)
- **メニューアイコンをモノクロ統一**: 一括操作メニュー・リスト表示の「⋮」メニューの「タグ追加」「ブックマーク追加」アイコンを、動画ページの操作ボタンと同じモノクロアイコンに変更。
- **スクリーンショット表示サイズ変更**: 動画ページのスクリーンショット欄にスライダーを追加。表示サイズを80px〜640pxの範囲で変更できます（画像本体は変更せず、縦横比は維持）。
- **一覧ページのリンクの中クリック対応**: カレンダー・ブックマーク・公開カレンダー・シリーズ一覧・出演者一覧・タグ一覧の各リンクを、中クリック（新しいタブで開く）やCtrl+クリックに対応しました。

### v1.4.1 (2026-06-28)
- **リスト表示の「⋮」メニューを変更**: リスト各行右端の「⋮」を、本家のタグ付けメニューから**動画ごとの操作メニュー**（タグ追加 / 詳細情報編集 / ブックマーク追加 / 動画の削除）に変更。選択モードの一括操作と同じ操作を1動画に対して行えます（グリッド表示の「⋮」は従来どおり）。
- **タグサムネイルをスクリーンショットから設定**: タグ一覧の「サムネイルを編集」で、当該タグが付いた動画の**スクリーンショットからランダム9枚**を表示して選択・設定できるように。「他の画像へ更新」で再抽選、「ローカルから選択」で従来どおりローカル画像の切り取り登録も可能。

### v1.4.0 (2026-06-26)
- **プリセットタグのグループ化**: 動画タグ・出演者タグの編集画面で、`#`／`##` を使ってタグをグループ化・ネストでき、グループ名の▼で折りたたみ表示。動画ページ（単体）のタグ編集も出演者と同じグループ対応ダイアログに統一。
- **出演者の関連動画数を表示**: 出演者一覧で各画像の右上に本数バッジ、出演者ページの「関連動画（N）」見出し。並び替えに「**出演数**」を追加（評価の右）。
- **リスト表示の情報拡張**: フォルダ名をサムネイル左下へ／元の位置に評価→スクショ数→ブックマークの順で表示／タグをウィンドウ幅に合わせて表示し、入り切らない分は「…」（マウスオーバーで全タグ）。
- **配布パッケージの構成変更**: zip 内に `server/ext/` を同梱し、`server` と `womanlive-ext.bat` をルートへコピーするだけで導入完了に。zip 名を `womanlive-ext_X.X.X.zip`（バージョン付き）へ変更。

### v1.3.0 (2026-06-25)
- 拡張つきで起動中だと分かるよう、ヘッダー表記を「**WomanLiveEX**」に変更し、ダーク/ライトの配色テーマを専用配色へ変更
- 動画ページのブックマークボタンを、「動画情報取得」「タグ編集」と同じ右下のボタン(FAB)に統一（上から ブックマーク → 動画情報取得 → タグ編集）
- 設定の拡張機能オプションの見出しを「拡張機能：…」（先頭にモノクロのパズルアイコン）へ統一
- ヘッダーの検索ボタンの右に**検索ヘルプ（？）**を追加（マウスオーバー／タップで本家・拡張の検索方法を表示）
- 動画/出演者の JSON 一括取込（MovieBrowser 形式）は配布パッケージから除外（個人用機能へ移行）

### v1.2.0 (2026-06-24)
- **タグ一覧ページ**を追加（登録動画のタグをサムネイル付きで一覧表示。既定は無地背景＋タグ名、各タグに推奨 16:9 のカスタムサムネイルを登録可。動画本数バッジ・タグ名／動画本数ソート・タグ名やサムネのクリックでタグ検索。ホームと右上メニューの「出演者一覧」の下にリンクを追加）
- **名前ソートのアルゴリズムを改善**（ひらがな・カタカナを同一の五十音順に統一／数字は桁数を考慮した自然順／先頭の記号は無視）。検索の「表示名」「名前」ソート、シリーズ一覧・出演者一覧・タグ一覧の名前ソートに適用

### v1.1.0 (2026-06-22)
- 検索結果に「表示名」ソートを追加（「名前」ソートの右に配置）
- 検索結果カードの表示を改善: メーカー名をサムネ右上に半透明表示／フォルダ名をサムネ左下（長さと同形式）へ／評価をフォルダ名の位置（スクショ枚数・ブックマークと同じ行）に表示
- 出演者・各メタデータの検索UIを統一（出演者の余分な虫めがねを削除し、各値に「〇〇で検索」ツールチップを追加）

### v1.0 (2026-06-18)
初版リリース。主な収録機能:
- カバー画像 / メタデータの拡張 / 関連動画（メタデータ類似・重み調整・事前計算）
- 出演者情報 / 出演者タグ・自動付与ルール
- 出演者一覧 /シリーズ一覧 / 公開年月別カレンダー
- 評価ソート・絞り込み / 各メタデータの検索 / 表示動画名検索
- スクリーンショット枚数の表示・枚数ソート 
- DMM(FANZA) 連携 / ブックマーク / 一括操作

</details>
